@model IEnumerable<Projekt_atrakcje.Models.Attraction>

@{
    ViewData["Title"] = "Atrakcje";
}

<h1 class="text-center">Atrakcje</h1>

<p class="text-center">
    <a class="btn btn-primary" asp-action="Create">Dodaj nową atrakcję</a>
</p>
<div class="table-responsive">
    <table class="table table-striped table-bordered text-center">
        <thead class="thead-dark">
            <tr>
                <th style="width: 150px;">@Html.DisplayNameFor(model => model.Name_attraction)</th>
                <th style="width: 150px;">@Html.DisplayNameFor(model => model.Category)</th>
                <th style="width: 150px;">@Html.DisplayNameFor(model => model.Description)</th>
                <th style="width: 150px;">@Html.DisplayNameFor(model => model.City)</th>
                <th style="width: 200px;">Akcje</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td style="width: 150px;" class="align-middle">@Html.DisplayFor(modelItem => item.Name_attraction)</td>
                    <td style="width: 150px;" class="align-middle">@Html.DisplayFor(modelItem => item.Category)</td>
                    <td style="width: 150px;" class="align-middle">@Html.DisplayFor(modelItem => item.Description)</td>
                    <td style="width: 150px;" class="align-middle">@Html.DisplayFor(modelItem => item.City.Name)</td>
                    <td style="width: 200px;" class="align-middle">
                        <div class="d-flex justify-content-between">
                            <a class="btn btn-info rounded-pill" asp-action="Details" asp-route-id="@item.Id">Szczegóły</a>
                            <a class="btn btn-warning rounded-pill ml-2" asp-action="Edit" asp-route-id="@item.Id">Edytuj</a>
                            <a class="btn btn-danger rounded-pill ml-2" asp-action="Delete" asp-route-id="@item.Id">Usuń</a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
